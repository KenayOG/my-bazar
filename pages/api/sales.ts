// pages/api/sales.ts
//import { NextApiRequest, NextApiResponse } from "next";

/* interface Sale {
  productId: number;
  quantity: number;
  date: string;
} */

//let sales: Sale[] = []; // Simulación de ventas, idealmente esto debería guardarse en una base de datos

/* export default function handler(req: NextApiRequest, res: NextApiResponse) {
  res.status(200).json(sales);
} */
